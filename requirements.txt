# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Quantization
quanto>=0.2.0

# Diffusers (for LTX-2)
diffusers>=0.36.0
transformers>=4.40.0
accelerate>=0.25.0

# Video processing
imageio>=2.31.0
imageio-ffmpeg>=0.4.8

# Utilities
tqdm>=4.65.0
numpy>=1.24.0
einops>=0.7.0
easydict>=1.10

# Hugging Face
huggingface-hub>=0.20.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# Narrow-precision compute backends (NVFP4 / FP8).
# Unlike quanto's weight-only INT4, these feed narrow types to the tensor cores and
# so reduce latency as well as VRAM. NVFP4 needs a Blackwell card (RTX 50-series);
# FP8 needs Ada (RTX 40-series) or newer. Optional -- the pipelines fall back to
# quanto INT4 with an explanation when torchao is absent.
# torchao>=0.9.0
